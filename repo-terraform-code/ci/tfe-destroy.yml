trigger:
  - master

resources:
  repositories:
    - repository: terraform-pipeline
      type: Git
      name: eminently-suitable-kingfish/terraform-pipeline
      ref: master

extends:
  template: pipeline/tfe-destroy-template.yml@terraform-pipeline
  parameters:
    isSpeculativePlan: False